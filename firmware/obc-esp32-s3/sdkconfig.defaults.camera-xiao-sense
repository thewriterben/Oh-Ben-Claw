# Camera-build sdkconfig overlay for the Seeed XIAO ESP32S3 Sense.
# NOT read by a default build, and NOT interchangeable with another board's.
#
# Renamed from `sdkconfig.defaults.camera` on 2026-09-16, when the LILYGO
# T-CameraPlus-S3 turned out to need QUAD PSRAM where this board needs OCTAL. The
# generic name was about to become the next default that is right for one board
# and silently wrong for the next.
#
# This file is selected only when ESP_IDF_SDKCONFIG_DEFAULTS names it:
#
#   $env:ESP_IDF_SDKCONFIG_DEFAULTS = "sdkconfig.defaults;sdkconfig.defaults.camera-xiao-sense"
#
# esp-idf-sys 0.37.2 reads that variable as a `;`-separated list
# (build/config.rs:25-27, parse::list at config.rs:187-202) and later files win.
# The variable REPLACES the default list rather than appending to it
# (set_when_none, config.rs:139-144), which is why `sdkconfig.defaults` is named
# explicitly above -- omit it and you lose the 32 KB main-task stack that was
# measured into existence on 2026-08-22.
#
# The point of the separate file: `sdkconfig.defaults` is global to every build
# from this tree, and forcing PSRAM on there would change the binary a default
# build produces -- including one flashed to the live mesh node. The env var is
# absent in a default build, so that isolation is mechanical, not a promise.
# See docs/DECISIONS.md (OBC-Prime), 2026-09-16.

# PSRAM holds the JPEG frame buffer.
#
# MEASURED 2026-09-16 on XIAO ESP32S3 Sense, MAC 64:E8:33:7E:7E:04, chip rev
# v0.2, 8 MB flash (gd), ESP-IDF v5.3.2. OCT is correct -- no longer a guess:
#
#   I (914) esp_psram: SPI SRAM memory test OK
#   I (998) esp_psram: Adding pool of 8192K of PSRAM memory to heap allocator
#   I (1309) cam_hal: Allocating 15360 Byte frame buffer in PSRAM
#
# The former TODO(source) is closed. QUAD was never tried and is not needed HERE;
# the LILYGO T-CameraPlus-S3 is quad, which is why these overlays are per-board.
CONFIG_SPIRAM=y
CONFIG_SPIRAM_MODE_OCT=y
CONFIG_SPIRAM_SPEED_80M=y
